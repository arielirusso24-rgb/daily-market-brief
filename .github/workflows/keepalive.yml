name: Keepalive

on:
  schedule:
    - cron: '0 12 1 * *'

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/keepalive-workflow@v1
